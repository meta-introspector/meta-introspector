[package]
name = "rust_preload_interceptor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
redhook = "2.0"
libc = "0.2"
ctor = "0.2"

[profile.release]
panic = "abort"
