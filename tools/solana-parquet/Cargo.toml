[package]
name = "solana-parquet"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "solana-to-parquet"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
arrow = "53"
parquet = { version = "53", features = ["arrow"] }
