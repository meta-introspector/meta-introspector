[package]
name = "burn-cuda-71"
version = "0.1.0"
edition = "2021"

[dependencies]
burn = { git = "https://github.com/tracel-ai/burn", features = ["cuda"] }
burn-cuda = { git = "https://github.com/tracel-ai/burn" }

# Force all cudarc dependencies to use 0.19.0 with CUDA 13 support
[patch.crates-io]
cudarc = "0.19.0"
