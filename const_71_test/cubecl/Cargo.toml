[package]
name = "cubecl-71"
version = "0.1.0"
edition = "2021"

# CubeCL is a GPU compute framework
# Full implementation would require:
# - cubecl-core, cubecl-cpu, cubecl-cuda, or cubecl-wgpu
# - LLVM bundler for compilation
# - Vulkan/CUDA for GPU execution
# 
# This test verifies the build system works
