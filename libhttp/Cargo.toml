[package]
name = "libhttp"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
reqwest = { version = "0.12", features = ["rustls-tls", "blocking"], default-features = false }
